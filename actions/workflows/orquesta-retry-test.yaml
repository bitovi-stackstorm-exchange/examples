## this action just hangs

version: 1.0

description: retry

vars:
  - loop: true

start:
  action: core.noop
  next:
    - do: task1

task1:
  action: core.noop
  next:
    - when: <% ctx("loop") = true %>
      publish:
        - loop: False
      do: task1